"use client";
import useSWR from "swr";

const fetchModels = () => fetch('/api/getEngines').then(res => r)

function ModelSelections() {
  return (
    <div>
      
    </div>
  )
}

export default ModelSelections
